# Published packages this example was tested against.
# agent-hooks-sdk resolves automatically as an agent-control-spec
# dependency; it is pinned here so the version is visible.
agent-control-spec==0.4.0a3
agent-hooks-sdk==0.1.0a5
pytest==9.1.1
pytest-asyncio==1.4.0
